[package]
name = "receiverproxy-sources"
description = "Frame sources: video files and test patterns"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
exclude.workspace = true

# Published as `receiverproxy-sources`; the library keeps its role name.
[lib]
name = "sources"

[dependencies]
wall.workspace = true
anyhow.workspace = true
ts-rs = { workspace = true, optional = true }

[features]
# `#[derive(TS)]` on `Fit` and `Pattern`, for web/src/api/types.ts.
ts = ["dep:ts-rs"]

[lints]
workspace = true
