[package]
name = "demos"
description = "Effects that lean on what an LED is, driven through the driver crate"
version.workspace = true
edition.workspace = true
license.workspace = true
# Not published: the example binary, not part of the `receiverproxy` install.
publish = false

[[bin]]
name = "rxp-demo"
path = "src/main.rs"

[dependencies]
wall.workspace = true
driver.workspace = true
sources.workspace = true

anyhow.workspace = true
clap.workspace = true
serde_json.workspace = true

[lints]
workspace = true
