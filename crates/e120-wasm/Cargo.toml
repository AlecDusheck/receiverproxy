[package]
name = "e120-wasm"
description = "e120-rcvbp and e120-canvas for the browser: generate, inspect and diff configs, check wall layouts"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[lib]
crate-type = ["cdylib"]

[dependencies]
e120-rcvbp.workspace = true
e120-canvas.workspace = true

anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
# Pinned to the installed wasm-bindgen CLI; the two must match exactly
# (docs/ui.md section 6).
wasm-bindgen = "=0.2.127"
serde-wasm-bindgen = "0.6"

[lints]
workspace = true
